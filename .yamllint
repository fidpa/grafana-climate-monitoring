# yamllint config — relaxed for readable ops files.
extends: relaxed
rules:
  line-length: disable
  document-start: disable
  comments:
    min-spaces-from-content: 1
  truthy:
    check-keys: false
